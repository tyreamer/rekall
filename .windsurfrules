COMPULSORY FIRST STEP: Before doing any work, get your bearings: call `rekall.brief` (MCP) or `rekall brief` (CLI). This returns current focus, blockers, failed attempts, and next actions. Log failed attempts with `rekall.attempt` (MCP) or `rekall attempts add` (CLI). Log decisions with `rekall.decision` (MCP) or `rekall decisions propose` (CLI). Checkpoint completed work with `rekall.checkpoint` (MCP) or `rekall checkpoint --commit auto` (CLI). End sessions with `rekall session end --summary '...'`. YOLO mode applies to execution speed, NOT the Rekall protocol.
