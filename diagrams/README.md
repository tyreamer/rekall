# Diagrams (v0.1)

These Mermaid diagrams support the Project State Layer specs.

- system_overview.mmd — the core architecture and link-out posture
- state_artifact_model.mmd — primitives in the state artifact
- coordination_flow.mmd — claim/lease + optimistic concurrency + append-only
- exec_query_flow.mmd — evidence-first executive Q&A

To render:
- GitHub will render Mermaid in Markdown contexts.
- Or paste the content into any Mermaid renderer.
